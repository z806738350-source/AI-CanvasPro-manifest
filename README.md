# AI-CanvasPro Manifest

Public update manifest for AI-CanvasPro.

This repository intentionally contains only non-sensitive release metadata. Runtime code is stored in the private `AI-CanvasPro-release` repository, and user data, `.env`, runtime binaries, and API keys must never be committed here.

Current manifest file:

- `update.json`
